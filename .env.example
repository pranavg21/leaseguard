# Copy to .env for local use. Never commit .env.
# Without GEMINI_API_KEY the app runs fully offline with deterministic rules.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# Optional: Firestore collection for anonymous usage counts (no document text is ever stored).
FIRESTORE_COLLECTION=
